// Write a program that calculates the area of a rectangle

function myRectangle(width: number, height: number): number{
    return width*height
}
// const width = 50
// const height = 50
// const area = myRectangle(width , height)
const area = myRectangle(50 , 200)
console.log(`The Total area is ${area} `)
// console.log("The width of rectangle is " + width + "and the height of rectangle is " + height + "and total area is" +  area)